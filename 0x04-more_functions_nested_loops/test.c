#include <stdio.h>

/**
 * main - Print perten
 *
 * Return: void
 *
 */
void main(void)
{
	int x, y, z;

	printf("enter the number of multiplier");
	scanf("%d", &x);

	for (y == 0; y <= x; y++)
	{
		for (z = 0; z <= y; z++)
		{
			printf("%d", z);
		}
	printf("\n");
	}
}
