#include "main.h"
/**
 * print_last_digit - prints the last digit of number.
 *
 * Return
 * @x: passed value
 */

int print_last_digit(int x)
{
	int r = x % 10;
if (x > 0)
{
	return (r);
	{
else if (x == 0)
{
	return (0);
else
{
return (r);
}
}
}
_putchar('\n');
return (0 + r);
	}

