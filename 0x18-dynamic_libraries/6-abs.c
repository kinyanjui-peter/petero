#include "main.h"
/**
 * _abs - Prints absolute value
 * @n: the passed value
 * Return: n
 *
 */

int _abs(int n)
{
        if (n > 0)
                return (n);
        else
                return (-n);
}
