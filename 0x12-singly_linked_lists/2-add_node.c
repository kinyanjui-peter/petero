#include <stdio.h>
#include "lists.h"
/**
 *
 *
 *
 */
list_t *add_node(list_t **head, const char *str)
{
	list_t *hptr;
	list_t *temp;

	temp = str
	list_t *temp = (list_t *)malloc(sizeof(list_t));
	if (temp)
	{
		return (&temp);
	}
	return (NULL);

