#include "main.h"

/**
 * reverse_array - prints inytergers in reverse
 * @a: input
 * @n: input
 * Return: 0;
 */

void reverse_array(int *a, int n)
{
	int x, y;

	for (x = 0; x < y; x++)
	{
	y = a[x];
a[x] = a[n];
a[n] = y;
	}
