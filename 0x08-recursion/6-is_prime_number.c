#include "main.h"
/**
 * is_prime_number - prints prime numbers
 *@n: maximum number
 *Return: 1 0r zero
 *
 */

int is_prime_number(int n)
{
	{
	if (n < 0)
		return (-1);
	if else (n / 1 == n && n / is_prime_number(int n) == n)
		return (1);
	else
		return (0);
	}
	return (0);
}
