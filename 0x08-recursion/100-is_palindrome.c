#include "main.h"
/**
 * is_palindrome - equate palindrome words
 * @s: passed string
 * Return: pointer to string/
 */
int is_palindrome(char *s)
{
	char rev;

	if (strlength < 0)
		return (-1)
	else
	{
rev = rev + *s[strlenth() - 1];
return (rev);
}

/**
 * strlength - find the length of the string
 */

int strlength(char*s)
{
	int x;

	x = 0;
	while (x != '\0')
	{
		l = s[x];
		x++;
	}
	return (l);
}

