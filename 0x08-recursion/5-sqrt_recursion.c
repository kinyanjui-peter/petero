#include "main.h"
/**
 * _prime_number - prints prime numbers
 *@n: maximum number
 *Return: 1 0r zero
 *
 */

int is_prime_number(int n)
{
	int x;

	if (n < 0)
		return (-1);
		if (n / 1 == n && n / (_prime_number(int n))  == n)
		return (1);
	else 
		return (0);
}
